//It may not be a good idea to use getters and setters, because the values are different and vales may be changes by another user between the calls
//so use a database to for futures transfer. using a trigger
